export enum MediaServerType {
  JELLYFIN = 'jellyfin',
}

export enum ArrServiceType {
  RADARR = 'radarr',
  SONARR = 'sonarr',
}

export enum MediaStatus {
  WATCHED = 'watched',
  PENDING_DELETE = 'pending_delete',
  DELETED = 'deleted',
  FAILED = 'failed',
}
